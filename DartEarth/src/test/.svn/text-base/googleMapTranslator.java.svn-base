package test;

import java.io.File;

public class googleMapTranslator {

	public static void main(String[] args) {
	/*
		String path = "";
		File file = new File(path);
		String[] tempList = file.list();
		for (int i = 0; i < tempList.length; i++) {
			File file1 = new File(path + "/" + tempList[i]);
			String[] tempList1 = file1.list();
			for (int j = 0; j < tempList1.length; j++) {
				// Image image = new Image(path+"/"+tempList1[j]);
				// Image newImage = new Image(null, image, SWT.IMAGE_GRAY);
			}

		}*/

		
	}
}
